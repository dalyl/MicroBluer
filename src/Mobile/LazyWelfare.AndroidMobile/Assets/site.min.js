// Write your JavaScript code.
$(function () {

    $(".ServiceScan").click(function () {
        var url = $(this).data("hybrid");
        location.href = "scan:" + url;
    });

    $(".HybridLink").click(function () {
        var url = $(this).data("hybrid");
        location.href = "hybrid:" + url;
    });

})

var ViewScript = {}
ViewScript.PartialLoad = function (parent,host,path)
{
    alert(123);
    var content = "";
    javascript: content = AndroidScript.PartialLoad(host, path);
    $(parent).html(content);
}